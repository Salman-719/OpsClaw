# ── Secrets (never commit) ────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=
OPENAI_API_KEY=

# ── OpenClaw settings (configured via CLI, not env vars) ─────────────────
# openclaw config set agents.defaults.model.primary openai/gpt-4o
# openclaw config set agents.defaults.workspace "/path/to/OpsClaw"
# "YOUR_KEY" | openclaw models auth paste-token --provider openai
# openclaw channels add telegram --token "$TELEGRAM_BOT_TOKEN"
